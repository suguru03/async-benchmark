machine:
  node:
    version: 5

test:
  override:
    - gulp init
    - node ./index.js each eachSeries eachLimit map mapSeries mapLimit parallel series parallelLimit waterfall auto autoInject retry race
